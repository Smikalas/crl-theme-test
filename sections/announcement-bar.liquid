<section class="announcement-bar-section announcement-bar-appear" style="padding-top: {{ section.settings.padding_top | default: 12 }}px; padding-bottom: {{ section.settings.padding_bottom | default: 12 }}px;">
  <div class="announcement-bar__inner">
    {% assign messages = section.blocks | where: 'type', 'message' %}
    {% if messages.size > 0 %}
      {% for block in messages %}
        <span class="announcement-bar__text" data-announcement-index="{{ forloop.index0 }}" style="display: {% if forloop.first %}inline{% else %}none{% endif %};">{{ block.settings.text }}</span>
      {% endfor %}
    {% else %}
      <span class="announcement-bar__text">{{ section.settings.announcement_text }}</span>
    {% endif %}
  </div>
</section>
<script>
(function() {
  var interval = {{ section.settings.message_interval | default: 5 }} * 1000;
  var messages = document.querySelectorAll('.announcement-bar__text[data-announcement-index]');
  if (messages.length < 2) return;
  var current = 0;
  setInterval(function() {
    messages[current].style.display = 'none';
    current = (current + 1) % messages.length;
    messages[current].style.display = 'inline';
  }, interval);
})();
</script>

{% stylesheet %}
.announcement-bar-section {
  width: 100%;
  background: #222;
  color: #fff;
  padding: 0;
  margin: 0;
  overflow: hidden;
}
.announcement-bar__inner {
  display: flex;
  justify-content: center;
  align-items: center;
  opacity: 0;
  transform: translateY(-30px);
  animation: announcement-bar-fade-in 1s cubic-bezier(0.4,0,0.2,1) forwards;
}
.announcement-bar__text {
  font-size: 1.1rem;
  font-weight: 500;
  letter-spacing: 0.05em;
  padding: 0 2rem;
  white-space: nowrap;
}
@keyframes announcement-bar-fade-in {
  to {
    opacity: 1;
    transform: translateY(0);
  }
}
{% endstylesheet %}

{% schema %}
{
  "name": "Announcement bar",
  "settings": [
    {
      "type": "text",
      "id": "announcement_text",
      "label": "Announcement text (deprecated)",
      "default": "🚀 Welcome to our store! Enjoy free shipping on all orders! 🚀"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Padding top (px)",
      "min": 0,
      "max": 60,
      "step": 2,
      "default": 12
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Padding bottom (px)",
      "min": 0,
      "max": 60,
      "step": 2,
      "default": 12
    },
    {
      "type": "number",
      "id": "message_interval",
      "label": "Message change interval (seconds)",
      "min": 2,
      "max": 30,
      "step": 1,
      "default": 5
    }
  ],
  "blocks": [
    {
      "type": "message",
      "name": "Message",
      "settings": [
        {
          "type": "text",
          "id": "text",
          "label": "Message text",
          "default": "New message"
        }
      ]
    }
  ],
  "max_blocks": 10,
  "presets": [
    {
      "name": "Announcement bar",
      "blocks": [
        { "type": "message", "settings": { "text": "🚀 Welcome to our store! Enjoy free shipping on all orders! 🚀" } },
        { "type": "message", "settings": { "text": "🎉 Summer Sale! Up to 50% off selected items! 🎉" } }
      ]
    }
  ]
}
{% endschema %}